package br.radixeng.model;

public class Main {

    public void main(Edge2 edge2, int maxStops ){

        source.getEdges().stream().forEach( e -> {
            e.getTarget().getName().equals(target.get)
        });
    }
}
